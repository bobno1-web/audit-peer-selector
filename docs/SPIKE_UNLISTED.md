# SPIKE_UNLISTED — 비상장 감사보고서 재무 추출 타당성 (Loop 0-D)

목표: 비상장 감사보고서에서 필요한 **8개 계정을 구조화 데이터로** 뽑을 수 있는가.
아래 숫자는 **실측**(실제 API 호출·파싱 집계)이다. 추론은 `[추측]`으로 명시한다.

## 조사 방법 (재현 가능)
- 스크립트: `scripts/spike_unlisted.py` (임시). `seed=20260714`.
- 표본: 비상장(114,560) 중 무작위 스캔 **301개** → **감사보고서 실제 제출 100개** 확보(제출율 ≈ 33%, 0-C의 38%와 정합).
- 경로1/2: 앞 50개 / 경로3(원문 파싱): 앞 30개.

## 경로별 성공률 (실측)

| 경로 | 방법 | 성공률 | 판정 |
|------|------|:---:|------|
| 경로1 | `fnlttXbrl.xml`(재무 원본 XBRL API)가 비상장 rcept_no에 응답 | **0.0%** (0/50) | ❌ 불가 |
| 경로2 | 경로1 XBRL 인스턴스에서 계정 파싱 | **0.0%** (파싱할 XBRL 없음) | ❌ 불가 |
| 경로3 | `document.xml`(공시원문) 표를 직접 파싱 | **100%** (30/30 최소 1개 계정) | ✅ 가능 |

→ **구조화 API(경로1/2)는 비상장에 무력. 원문 표 파싱(경로3)만 실효.**

## 8개 계정별 추출 성공률 (경로3, n=30, 실측)

| 계정 | 성공률 | (건수) | 70% 기준 |
|------|:---:|:---:|:---:|
| 매출액 | 96.7% | 29/30 | ✅ |
| 총자산 | 96.7% | 29/30 | ✅ |
| 매출원가 | 90.0% | 27/30 | ✅ |
| 매출총이익 | 86.7% | 26/30 | ✅ |
| 영업이익 | 83.3% | 25/30 | ✅ |
| 재고자산 | 83.3% | 25/30 | ✅ |
| **매출채권** | **66.7%** | 20/30 | ❌ (미달) |
| **종업원수** | **0.0%** | 0/30 | ❌ (미달) |

- 재무제표 본문 7개 계정 중 6개는 **83~97%**로 잘 나온다.
- **매출채권 66.7%**: `[추측]` "매출채권및기타채권"으로 합산·순액 표기되어 키워드 매칭에서 일부 누락된 것으로 보임(파서 한계 가능).
- **종업원수 0%**: 감사보고서 재무제표에 **없는 항목**(상장사는 사업보고서 '임직원 현황'에 있음). 실측·구조적으로 확인된 결측.

## 결측 편향 (실측)
- 핵심 2계정(매출액·총자산)을 모두 추출한 케이스 **29/30**, 실패 **1/30**.
- 성공 케이스는 다양한 업종(제조 20·24·25·28·29, 도소매 46, 건설 41, 부동산 68, 전문서비스 74 등)에 분포. 실패는 업종 41(건설) 1건뿐.
- → **특정 산업/규모 쏠림 없음.** 단 `[추측]` 표본 30이라 검정력은 약하다(넓은 신뢰구간).

## 사업내용 텍스트 대체재 (실측 + 추측)
- 감사보고서에 사업설명 텍스트 존재: **96.7%** (29/30) — "회사의 개요"/주석 형태.
- `[추측]` 상장사 "사업의 내용"만큼 **구조화·상세하진 않다.** 비교가능 수준인지의 정량 비교(길이·항목)는 이 스파이크에서 하지 않음 → **미확인**.

## 추정과 실측 구분 요약
- **실측**: 위 모든 % (경로별·계정별·텍스트 존재율·결측 분포). 실제 호출/파싱 집계.
- **추측/한계**: (a) 경로3 파서의 오탐·누락 여지, (b) 매출채권 저조의 원인, (c) 표본 30의 신뢰구간, (d) 텍스트 "비교가능 수준" 판단.

## GO/NO-GO 적용 (기준은 LOOP_0D.md PART E에 결과 전 고정)
- 기준(GO): **8개 계정 전부 ≥ 70%** AND 결측이 산업/규모에 심하게 쏠리지 않음.
- 결과: 결측 편향은 없음(조건 충족). 그러나 **매출채권 66.7%, 종업원수 0%**로 두 계정이 70% 미달 → 계정 조건 **미충족**.
- AND 결합 → **NO-GO**. (기준을 결과에 맞춰 늘리지 않는다. 애매하면 NO-GO.)

→ 상세 결정: `docs/DECISIONS.md` **D-006**.

---

## [Loop 0-E 재측정] n=200, 필수 6계정, 개선 파서 (실측)
원자료: `runs/2026-07-14_spike_unlisted_v2/` (summary.json, cases.csv, config.yaml). 종업원수 오류 철회 후
필수 6계정만 대상. 파서·사전은 `scripts/audit_parser.py` + `config/account_aliases.yaml`.

**계정별 성공률 (n=200)**: 매출액 93.0 · 매출원가 87.5 · 영업이익 80.5 · 매출총이익 79.0 ·
매출채권 76.0 · 재고자산 72.0 (%). → 개별은 6개 전부 ≥70%.

**★ 엄격 성공률(필수 6계정 전부 확보) = 51.0% (102/200).** (D-3: 관대한 정의 금지, 이것이 기준 지표.)

**결측 편향(D-4)**: 실패가 **부동산(induty 68) 32건·건설(41) 12건**에 쏠림(부동산 성공 5 vs 실패 32).
→ 특정 산업 쏠림 **있음**.

**매출채권 원인규명**: 파서 66.7% / 데이터 33.3% (`docs/RECEIVABLE_DIAGNOSIS.md`). 사전 확장으로 73%→76%.

→ 최종 판정: `docs/DECISIONS.md` **D-008 = NO-GO** (엄격 성공률 51%<70% + 결측 쏠림).

# Loop 5: 부문별 매출 완결 + 계정 매핑 독립 검증

## 위치
C:\Users\gmg97\Desktop\25-26년\동종업계탐색기_클로드

## 이번 루프의 의미
마지막 남은 무료 축(부문별 매출)을 완결하고, 3루프째 미뤄온 계정 매핑 독립 검증을 상환한다.
이 둘이 끝나면 holdout 개봉 준비가 된다.
★ holdout(2023~2025)은 이번에도 열지 않는다.
★ 부문별 매출이 효과 없어도 정직히 기록. 성능을 위해 데이터를 왜곡하지 마라.

## 순서
PART 0(API 예산) → [트랙 A: PART 1-2 부문] ∥ [트랙 B: PART 3 매핑검증] → PART 4(재조합·결판) → PART Z

## 세션 한도 지침
- 서브에이전트는 독립·장시간 작업에만. 잘게 쪼개 세션 낭비 금지.
- 병렬 후보는 딱 둘: 트랙 A(부문 수집·API 대기) ∥ 트랙 B(매핑 검증·독립). 나머지 순차.
- API 호출 전 예상 호출 수 로그, 캐시 우선. 020 소진 시 중단·재개(resume). 억지 반복 금지.

═══════════════════════════════════════
## PART 0. API 예산 사전 점검
═══════════════════════════════════════
0-1. 020 일한도 잔량 확인 (probe 1회)
0-2. 부문 데이터 수집 예상 호출 수 계산·로그
0-3. ★ raw 섹션 캐싱 도입 (검증방 Loop4 지적 #2). document.xml raw 섹션(표 구조 포함) 캐시.
     캐시 키 = (기업, 연도, 섹션, 원본해시). 다음부터 020 리셋마다 재취득 0.

═══════════════════════════════════════
## 트랙 A ── PART 1. 부문별 매출 수집
═══════════════════════════════════════
1-1. 사업보고서 부문(segment)별 매출 수집. 한도 내 최대, 부족분 resume 이월. raw 섹션 캐시 저장.
1-2. 수집 성공률 측정·보고 (몇 % 기업 부문표 확보).
1-3. ★ 결측은 결측 (임의대체 금지).

## 트랙 A ── PART 2. 부문 축 구성
2-1. 각 기업 = 부문 비중 벡터. 공통 축 정규화 명시(HHI/top_share/seg_n 스칼라).
2-2. ★ 정보 차단벽: 부문 "매출 비중"만. 부문 "이익률" 금지. test_no_leakage 통과.
2-3. docs/SEGMENT_EXTRACTION.md 갱신 (성공률 + 정규화 + 품질 표본).

═══════════════════════════════════════
## 트랙 B ── PART 3. 계정 매핑 독립 검증  (3루프째 숙제 상환)
═══════════════════════════════════════
3-1. 매핑 별칭 독립 재유도. 표본 30+ 기업 raw DART 재무제표를 열어 "우리가 고른 계정이
     정말 그 비율의 올바른 분자·분모인가" 육안 대조. 특히 매출채권 "및기타채권" 합산선(67%),
     매출원가←영업비용(5%).
3-2. 불일치 분류: (a)명백한 오매핑=심각·수정 (b)허용 근사=기록·유지 (c)정의 차이(연결/별도)=기록.
3-3. docs/MAPPING_AUDIT.md: 표본별 정확성 + 오매핑 비율·유형 + ★판정(신뢰 가능한가?).
     심각 오매핑 시 그 별칭만 교정(ORACLE 비율 불변). 수정 전후 점수 영향 보고.
3-4. ★ 원자료 runs/ 커밋 (검증방 재대조 가능하게).

═══════════════════════════════════════
## PART 4. 재조합 + 결판  (dev에서만)
═══════════════════════════════════════
4-1. engines/similarity/ 부문 축 추가 (활성 축 = 산업·규모·시총·텍스트·성장·부문).
4-2. dev 재학습 (holdout 미사용, 동일 학습절차·케이스셋·상수).
4-3. 일곱 나란히: 시장/랜덤/baseline/L2/L3/L4/L5. APE 중앙값 + 비율별 + within + coverage.
     firm-clustered 부트스트랩 유의성.
4-4. ★ 판정: L5가 L4(0.4965) 이겼나? 유의? 부문 축 기여? 텍스트 대체/보완? 매핑 수정 영향?
     승리조건(0.433) 충족 여부.
4-5. docs/SHOWDOWN_L5.md: 일곱 점수, 축별 기여, 정직한 결론(무료 축 소진, 남은 격차·상한),
     holdout 개봉 권고(무료 카드 소진? 결정은 사람).

═══════════════════════════════════════
## PART Z
═══════════════════════════════════════
Z-1. 발견 라우팅. 미반영 0건.
Z-2. 검증방 Loop4 지적 상환 확인: 매핑 독립검증(PART3) / raw 섹션 캐싱(PART0) /
     holdout feature 빌드 격리(build가 홀드아웃 연도 생성 — 격리하거나 이유 명시).
Z-3. LOOP_LOG.md Loop 5 기록 (비개발자용 한 줄).

═══════════════════════════════════════
## 금지 사항
═══════════════════════════════════════
- holdout(2023~2025) 열기 금지 ★
- API 한도 억지 반복 호출 금지 (resume 이월)
- 부문 "이익률" 사용 금지 (매출비중만)
- 매핑 수정 시 ORACLE 비율 자체 변경 금지 (별칭만 교정)
- 학습 절차·케이스셋·상수 유리하게 변경 금지 / 가중치 holdout 학습 금지
- 결측 임의대체 금지 / 부문 추출 특정회사 하드코딩 금지 (구조적 기준)
- engines↛scoring/targets, 룩어헤드 0 / 매직상수·리터럴 금지
- 서브에이전트 남발 금지 (독립·장시간 작업만)

═══════════════════════════════════════
## 완료 조건 P1 ~ P22
═══════════════════════════════════════
[API 예산]
P1.  020 잔량 probe + 예상 호출 수 로그
P2.  raw 섹션 캐싱 구현 (재취득 방지), 캐시 히트/미스 보고
P3.  API 호출 예산 내, resume 로직 존재

[부문별 매출]
P4.  부문 매출 수집, 성공률 실측 보고
P5.  부문 비중 벡터 정규화 방법 명시
P6.  부문 이익률 미사용 (매출비중만), test_no_leakage 통과
P7.  결측 임의대체 0
P8.  SEGMENT_EXTRACTION.md 갱신

[매핑 검증]
P9.  표본 30+ raw DART로 매핑 독립 재유도
P10. 오매핑 비율 + 유형 분류
P11. MAPPING_AUDIT.md: 매핑 신뢰성 판정
P12. 심각 오매핑 시 별칭만 교정 (ORACLE 비율 불변), 수정 전후 영향 보고
P13. 매핑 검증 원자료 runs/ 커밋

[재조합·결판]
P14. similarity에 부문 축 추가, config
P15. dev 학습·holdout 미사용, 축별 가중치 보고
P16. 동일 학습절차·케이스셋·상수 (유리하게 변경 0)
P17. engines↛scoring/targets, 룩어헤드 0 (테스트 3종 통과)
P18. 일곱 대조군 나란히, firm-clustered 부트스트랩
P19. SHOWDOWN_L5: 축별 기여 + 승리조건 + holdout 개봉 권고 + 미개봉 확인

[불변]
P20. ORACLE 4비율 그대로 (별칭 교정은 비율 정의 불변)
P21. 엔진↛채점기 격리 / holdout 미개봉
P22. PART Z 미반영 0 (매핑·캐싱·격리 상환 확인)

## 진행 상태 — ★ 전 PART 완료
- PART 0(API 예산·raw캐시): ✅  P1(probe·예산로그) P2(raw캐시·7test) P3(resume)
- 트랙 A(부문 수집·축): ✅  P4(85.7%) P5(HHI/top_share/seg_n) P6(no_leakage) P7(무대체) P8(SEGMENT_EXTRACTION)
- 트랙 B(매핑 독립검증): ✅  P9(40기업 XBRL id) P10(0.43%) P11(신뢰판정) P12(불변·영향0) P13(runs/ 커밋)
- PART 4(재조합·결판): ✅  P14(6축config) P15(가중치보고) P16(동일절차) P17(테스트3종) P18(일곱·clustered) P19(SHOWDOWN_L5)
- PART Z: ✅  P20(ORACLE불변) P21(격리·미개봉) P22(매핑·캐싱·격리 상환)
- ★ 결과: L5 0.5000 (L4 0.4965 무차별, 부문 기여≈0). 승리조건 0.433 미달. 무료 카드 소진. holdout 미개봉.

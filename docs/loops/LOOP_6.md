# Loop 6: 예측방식 개선(B) + 예측불가 기업 분리(C) + 신경망 무료 정찰

## 위치
C:\Users\gmg97\Desktop\25-26년\동종업계탐색기_클로드

## 배경
지금까지 "peer를 고르는 축"만 바꿨고 벽에 도달했다(L4 0.4965).
아직 안 건드린 두 방향:
  B. 고른 peer로 "어떻게 예측하는가" (지금은 단순 중앙값)
  C. 애초에 이 방식으로 "예측 불가능한 기업"을 분리
+ 신경망 임베딩을 무료 소형 모델로 정찰만 (유료 지출 전 최종 판단).
★ holdout(2023~2025) 이번에도 열지 않는다.
★ 매핑 버그(검증방 Loop5 발견)도 함께 교정.

## 순서
PART 0(버그 교정) → PART 1(예측방식 B) → PART 2(기업분리 C)
→ PART 3(신경망 무료 정찰) → PART 4(결판) → PART Z

═══════════════════════════════════════
## PART 0. 매출채권 매핑 버그 교정 (Loop 5 검증방 발견)
═══════════════════════════════════════
0-1. _is_pure_receivable 교정: "tradereceivable"(단수) 매칭 + otherreceivable 배제.
     지목 사례(01259418/2018, 00141389/2020)로 확인.
0-2. 별칭 우선순위: 순수 매출채권 > 합산. resolver에 specificity 규칙. D-025 사전등록. ORACLE 비율 정의 불변.
0-3. targets 재생성 + 변경 기업·크기 보고.
0-4. candidates.csv 커밋.
★ B·C·정찰의 채점 기반이므로 먼저.

═══════════════════════════════════════
## PART 1. 예측 방식 개선 (B)
═══════════════════════════════════════
1-1. 변형 dev 비교 (peer 선정 L4 고정, 예측만 변경):
     (a) 단순 중앙값(기준) (b) 유사도 가중 평균 (c) 유사도 가중 중앙값
     (d) 극단값 절사 평균 (e) k=3/5/7/10 변화. k·방식 config, 매직상수 금지.
1-2. 정보 차단벽 유지: 예측은 peer의 채점비율 값만(원래 채점 단계). test_no_leakage 통과.
1-3. 각 변형 dev APE 보고. 최선 변형 식별.

═══════════════════════════════════════
## PART 2. 예측 불가능 기업 분리 (C)
═══════════════════════════════════════
2-1. "예측 불가" 판정 = 재무구조(분모 계정 0 근처)로만. 채점비율 실제값 미사용(정답 훔쳐보기 금지).
     기준값 dev 유도. 임의상수 금지. D-027 사전등록.
2-2. (전체) 모든 기업 APE + (안정) 예측불가 제외 APE 둘 다 보고. 제외 비율 명시. >20%면 한계로 기록.
2-3. 감사 활용: "비교 부적합" 플래그 문서화 (red-flag 아님).

═══════════════════════════════════════
## PART 3. 신경망 임베딩 무료 정찰
═══════════════════════════════════════
3-1. 무료 소형 로컬 임베딩 모델 확보 시도. 네트워크 정책상 불가면 "환경제약" 명시, 유료 API 금지.
3-2. 가능하면 "사업의 내용" 무료 임베딩 벡터화 → TF-IDF(0.31) 대비 텍스트 가중치 오르나? 캐싱 필수.
3-3. docs/EMBEDDING_SCOUT2.md: 무료 신경망 vs TF-IDF + 유료 가치 판정(높음/중간/낮음).

═══════════════════════════════════════
## PART 4. 결판 (dev에서만)
═══════════════════════════════════════
4-1. L6 = L4 peer선정 + 최선 예측방식(B) + 예측불가분리(C).
4-2. 나란히 (전체/안정 둘 다): 시장/랜덤/baseline/L2/L3/L4/L6. 비율별/within/coverage/firm-clustered 부트.
4-3. 판정: B 개선? C 후 안정 APE·제외비율? 정찰 판정? 승리조건 0.433 (전체/안정 각각).
4-4. docs/SHOWDOWN_L6.md: B·C로 벽 넘었나 vs 상한 재확인. holdout 개봉 권고.

═══════════════════════════════════════
## PART Z
═══════════════════════════════════════
Z-1. 발견 라우팅. 미반영 0.
Z-2. Loop 5 검증방 지적 상환(매핑 버그·별칭 우선순위·candidates 커밋).
Z-3. LOOP_LOG.md Loop 6 기록.

═══════════════════════════════════════
## 금지 사항
═══════════════════════════════════════
- holdout(2023~2025) 열기 금지 ★
- 예측불가 분리를 타겟 채점비율 실제값으로 판정 금지 → 재무구조(분모 0근처)로만, dev 유도
- 예측불가 기업 몰래 빼서 점수만 올리기 금지 (전체·안정 둘 다 보고)
- 예측방식·k·분리기준 holdout으로 정하기 금지 (dev만)
- 무료 정찰 안 되면 억지로 유료 API 금지
- ORACLE 비율 정의 변경 금지 (별칭 해석만 교정)
- engines↛scoring/targets, 룩어헤드 0, 매직상수 금지

═══════════════════════════════════════
## 완료 조건 Q1 ~ Q22
═══════════════════════════════════════
[버그 교정]
Q1.  _is_pure_receivable 교정, 지목 사례 확인
Q2.  별칭 우선순위(순수>합산), D-025 사전등록
Q3.  ORACLE 비율 정의 불변
Q4.  targets 재생성, 변경 보고, candidates.csv 커밋
[예측방식 B]
Q5.  변형 (a)~(e) dev 비교, 각 APE 보고
Q6.  k·방식 config (매직상수 0)
Q7.  예측방식 dev에서만 선택 (holdout 미사용)
Q8.  test_no_leakage 통과 (엔진 채점비율 미접근)
Q9.  최선 변형 식별
[기업분리 C]
Q10. 예측불가 기준 재무구조 기반 (채점비율 실제값 미사용) ★
Q11. 기준값 dev 유도, D-027 사전등록
Q12. 전체 APE + 안정 APE 둘 다, 제외 비율 명시
Q13. 감사 활용(비교부적합 플래그) 문서화
[신경망 정찰]
Q14. 무료 모델 확보 시도 (불가 시 환경제약 명시, 유료 미사용)
Q15. 가능 시 TF-IDF 대비 비교, EMBEDDING_SCOUT2.md 판정
Q16. 캐싱 적용
[결판]
Q17. L6 = L4선정+최선예측+기업분리, 일곱+ 대조군 나란히
Q18. 전체/안정 APE 둘 다, firm-clustered 부트스트랩
Q19. SHOWDOWN_L6: B·C 효과 + 정찰 판정 + 승리조건 + holdout 권고
Q20. holdout 미개봉 확인
[불변]
Q21. engines↛scoring/targets / 룩어헤드 0 / ORACLE 비율 불변
Q22. PART Z 미반영 0

## 출력
1. 버그 교정 + 변경 기업 수 + 상대순위 유지 확인
2. 예측방식 B: 어느 변형 최선? 얼마나 개선?
3. 기업분리 C: 안정 APE + 제외 비율
4. 신경망 무료 정찰: 유료 가치 판정
5. L6 최종 dev 점수 (전체/안정) + 승리조건
6. 정직한 결론: 벽 넘었나 + holdout 개봉 준비됐나
7. holdout 미개봉 확인
8. Q1~Q22 자가 체크
9. 제안 (만들지 말고 제안만)

## 진행 상태 — ★ 전 PART 완료
- PART 0(버그교정): ✅ Q1(단수매칭·00141389 확인) Q2(별칭우선순위·D-026) Q3(ORACLE불변) Q4(767 매출채권
  교정·중립·candidates 커밋). raw_fin_cache(+6test). 매핑감사 22 OK/17 DISCLOSURE/1 NO_PICK.
- PART 1(예측 B): ✅ Q5(변형 dev비교) Q6(config·매직0) Q7(dev만) Q8(no_leakage) Q9(최선=median@k10).
  median k5→k10 0.4977→0.4794, 가중평균 악화. D-028.
- PART 2(분리 C): ✅ Q10(재무구조 기반) Q11(dev유도·D-027) Q12(전체0.4794/안정0.4452·제외9.9%) Q13(비교부적합 플래그).
- PART 3(정찰): ✅ Q14(model2vec 무료·로컬) Q15(TF-IDF 대비·EMBEDDING_SCOUT2) Q16(벡터 캐시). 유료가치 LOW.
- PART 4(결판): ✅ Q17(일곱+·L6=L4선정+median@k10) Q18(전체/안정·clustered 부트) Q19(SHOWDOWN_L6) Q20(미개봉).
- PART Z: ✅ Q21(격리·룩어헤드0·ORACLE불변) Q22(라우팅 미반영0, 검증방 Loop5 상환).
- ★ 결과: **L6 0.4794**(안정 0.4452). **L4 0.4977 대비 −3.68%, firm-clustered 부트 유의**(3루프 만에 벽
  돌파). 원천 = peer 5→10(예측 head), 집계함수 아님. 승리조건 0.433 미달(gap 좁혀짐). 임베딩 LOW. holdout 미개봉.

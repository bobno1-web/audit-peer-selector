# Loop 3: 텍스트 피처 정밀 재도전 + 시총 축 추가

## 위치
C:\Users\gmg97\Desktop\25-26년\동종업계탐색기_클로드
완료조건 N1~N18.

## 이번 루프의 의미
Loop 2에서 사업내용 텍스트가 무효였다(가중치 0).
원인 가설: 사업보고서 전문을 flatten TF-IDF로 써서 사업모델 신호가
재무·주석·법률 노이즈에 묻혔다.
이번엔 "사업의 내용" 섹션만 정밀 추출하고, 시총 축도 추가해 재조합한다.
목표: baseline(0.5416) 대비 개선. 승리조건 0.433.
holdout(2023~2025)은 이번에도 열지 않는다. dev에서만.

## 순서
  PART 0(비용 가드) → PART 1(텍스트 정밀추출) → PART 2(시총)
  → PART 3(재조합·재학습) → PART 4(결판) → PART Z

## PART 0. API 비용 가드
0-1. .claude/rules/api-budget.md 신규
0-2. 임베딩 캐시 계약 (캐시 키 = (기업,연도,섹션,모델버전) 해시; 새 캐시로 분리; 히트/미스 보고)
0-3. tests/test_embedding_cache.py 신규 (재요청 시 호출 0 실증)

## PART 1. "사업의 내용" 정밀 추출
1-1. 섹션만 추출하는 파서 (구조적 기준, 하드코딩 금지, 실패는 결측)
1-2. 20개 표본 육안검사 → docs/TEXT_EXTRACTION.md
1-3. 텍스트 → 벡터 (캐싱; 전문 TF-IDF와 차이 문서화)

## PART 2. 시총 축 추가
2-1. 시총 as_of 시점 (룩어헤드 금지)
2-2. 규모 축과 상관 점검 (중복 여부 정직 보고)

## PART 3. 재조합 + 가중치 재학습
3-1. similarity 확장: 산업/규모/시총/텍스트(정밀) 4축
3-2. dev에서만 재학습, Loop 2와 동일 절차, 가중치 보고
3-3. 절대규칙 준수 (engines↛scoring/targets, 룩어헤드 0, 매직상수 0, 테스트 3종)

## PART 4. 결판 (dev만)
4-1. 동일 조건 (k=5, q=0.9, min=3)
4-2. 다섯 나란히: 시장중앙값/랜덤/baseline/similarity(L2)/similarity+텍스트(L3)
4-3. 판정 (텍스트 개선? 가중치 0 벗어남? 시총 기여/중복? 승리조건?)
4-4. docs/SHOWDOWN_L3.md

## PART Z
Z-1. 발견 라우팅, 미반영 0건
Z-2. 검증방 미상환 항목: 매핑 정확성 독립검증 / 부트스트랩 유의성
Z-3. LOOP_LOG.md Loop 3 기록

## 금지 사항
- holdout(2023~2025) 열기 금지
- 임베딩 캐시 없이 대량 호출 금지
- 사업보고서 전문 임베딩 금지 (섹션만)
- 학습 절차·케이스셋·상수 유리하게 변경 금지
- 가중치 holdout 학습 금지
- 섹션 추출 특정회사 하드코딩 금지 (구조적 기준)
- engines↛scoring/targets, 룩어헤드 0
- 매직상수/리터럴 금지
- ORACLE 4비율 변경 금지

## 완료 조건 N1 ~ N18
N1.  .claude/rules/api-budget.md 존재
N2.  임베딩 캐시 구현, 캐시 히트/미스 보고
N3.  test_embedding_cache.py 존재, 재요청 시 호출 0 실증
N4.  "사업의 내용" 섹션 추출 파서, 추출 성공률 보고
N5.  섹션 식별 구조적 기준 (특정회사 하드코딩 0)
N6.  추출 실패는 결측 처리 (임의대체 0)
N7.  docs/TEXT_EXTRACTION.md: 성공률 + 품질 육안검사
N8.  임베딩 캐싱됨, 전문 대비 차이 문서화
N9.  시총 as_of 시점 기준 (룩어헤드 0)
N10. 규모 축과 상관 점검, 중복 여부 보고
N11. similarity 확장: 산업·규모·시총·텍스트(정밀) 4축
N12. dev에서만 학습, holdout 미사용, 가중치 보고
N13. Loop 2와 동일 학습 절차 (유리하게 변경 0)
N14. engines↛scoring/targets, 룩어헤드 0 (테스트 3종 통과)
N15. 다섯 대조군 나란히, 동일 조건
N16. 텍스트 가중치가 0을 벗어났는가 명시 + 비율별 분해
N17. SHOWDOWN_L3.md 정직한 결론, 승리조건 판정, holdout 미개봉
N18. ORACLE 4비율 / PART Z 미반영 0

## 출력
1. 예상/실제 API 호출 수 (캐시 히트/미스)
2. "사업의 내용" 추출 성공률 + 품질
3. 다섯 대조군 점수 나란히
4. 텍스트 가중치가 이번엔 0을 벗어났는가?
5. 시총이 기여했나 규모와 중복인가
6. 승리조건(0.433) 충족 여부 + 정직한 결론
7. holdout 미개봉 확인
8. N1~N18 자가 체크
9. 제안 (만들지 말고 제안만)

# LOOP_WEB10 — 태그 절대하한(①) + 2025 데이터 수집(②)

## 프로젝트 (확인)
`동종업계탐색기_클로드` (PeerLens / peer-selection). ★ DartLens 참조면 다른 프로젝트 — 중단.

## 두 독립 작업
- ① 태그 절대하한(표시 계층): "사업내용 유사" 등이 안 비슷해도 붙는 오판정 수정. 하한을 **dev 데이터에서 도출**(하드코딩 금지).
- ② 2025 회계연도 데이터 수집(데이터 계층): as_of 2026-05-15 → fiscal 2025. 룩어헤드 0. **확정 엔진 apply(재학습 0)**.
- ①=메인, ②=서브에이전트(독립·장시간). 합류: ①하한을 ②새 스냅샷에 적용(마지막만 순차).

## ① 태그 절대하한
- ①-A 원인: 태그 로직이 rationale 상위2축(v>0)이면 라벨 — 절대 하한 없음. 텍스트 코사인이 사업보고서
  상투어 겹침을 잡아 식품↔전선도 "사업내용 유사". (오뚜기←대한전선/KG스틸, CJ←삼성전기/두산에너빌리티 실측.)
- ①-B 하한 도출: dev 데이터에서 같은-KSIC 쌍 vs 다른-KSIC 쌍의 텍스트 코사인 분포 비교 →
  "같은 산업 수준 유사"의 경계를 데이터로. 근거·값 `docs/TAG_THRESHOLD.md` 기록(재현 가능). 축별 하한 필요성 검토.
- ①-C 로직 수정(표시만): 상위2축이어도 raw sim < 하한이면 그 라벨 제거. ★ 순위·유사도 점수 불변(라벨만).
  하한 미달로 태그 다 빠지면 그룹2 또는 태그없이(그룹 규칙 정합).
- ①-D 정직성: "안 비슷한 걸 유사라 안 부르기". 하한은 데이터 근거 필수. 진짜 유사까지 과도 제거되면 재검토.

## ② 2025 수집
- ②-A as_of 2026-05-15, fiscal 2025까지만. DART 한도 probe + 예상 호출 로그 + resume.
- ②-B 기존 수집 파이프라인 재사용(신설 0). 피처화 dev 동일. 임베딩 캐싱(신규만). 텍스트 캐시 2025로 갱신(낡은 2016 대체).
- ②-C 확정 엔진 apply: dev 확정 weights 로드만, fit 금지. weights 해시 dev 동일 확인.

## 합류
H-1 ①하한을 ②새 스냅샷 적용. H-2 하한 2025 타당성 확인. H-3 CJ 재확인(오판정 사라짐·식품 유지). H-4 웹 최신 스냅샷 조회(Web-5 방식, 화면 무변경).

## 유지/금지
유지: Web-8/9 디자인, 3페이지 라우팅, 엔진·채점기·ORACLE, 순위·유사도 원본, 2022·2024 스냅샷(롤백).
금지: 하한 임의값(데이터 도출만) / 최신데이터 재학습(apply만·해시동일) / 순위·점수 조작 / ORACLE·엔진 변경 / 룩어헤드 / 캐시없는 대량재호출 / 키 저장 / 다른 프로젝트.

## 완료 조건 T1~T20
① T1 원인재확인 · T2 하한 dev도출+TAG_THRESHOLD.md · T3 임의값아님(분포·상관) · T4 축별 필요성 · T5 로직에 하한(라벨만·점수불변) ·
  T6 CJ 오판정 사라짐·식품유지 · T7 미달peer 그룹2/태그없이 · T8 과도제거 없음.
② T9 as_of2026·fiscal2025·룩어헤드0 · T10 기존파이프재사용 · T11 수집률·resume·캐시 · T12 서브에이전트 병렬 · T13 텍스트캐시2025 · T14 재학습0·해시동일 · T15 2025스냅샷·형식동일.
합류 T16 하한 2025적용·타당성 · T17 최신+하한 CJ재확인 · T18 웹최신·화면무변경.
불변 T19 엔진·채점기·ORACLE·순위·점수·2022/2024롤백 · T20 다른프로젝트.

## 상태 (완료)
- **① 완료 / ② 완료(core+text) / 합류 완료.** 전체 테스트 100 pass. 웹 2026 서빙, 화면 무변경.
- 핵심: 태그 하한은 **벡터공간별 유도**(METHOD 동결=cross-KSIC2 널 p99). 공유/dev 공간=0.52,
  프레시 2026 공간=0.49(H-2 발견). 엔진·순위·점수·ORACLE·templates 0변경(git diff), 2022/2024 롤백 보존.

### (이전 진행 기록)
- **① 태그 하한 — 완료.** floor=0.52 를 dev(2016~2022) 널 p99 로 독립 재도출(mean 0.5200, std 0.0027,
  AUC 0.653). `scripts/derive_tag_floor.py`·`docs/TAG_THRESHOLD.md`·`config/tag_thresholds.json` 결속
  (이전 config 의 삭제된 scratchpad 출처 → 커밋 스크립트로 교정). `web_engine` 표시계층에 적용(라벨만).
  dev(2022) query==Loop8 sample 완전일치(엔진 불변 실증). CJ/오뚜기 오판정 제거·식품 보존 확인.
  `tests/test_tag_floor.py`(4) + WEB-1 서빙계약 정정. **전체 100 pass.**
- **② 2026 스냅샷(fiscal 2025) — core 완료 + 텍스트 수집 중(백그라운드).** universe_2026=2833(상장),
  scale/industry/mktcap(94.3%)/growth(70.9%)/ratios 완료. as_of 2026-05-15, 룩어헤드 0. 가중치 해시
  2e48a3cd(동결 L6 동일, 재학습 0). `build_snapshot_year.py`(연도확장 재사용). 텍스트 최신화(FY2025
  '사업의 내용' → section_vectors_2026.npz, 별도파일) 진행 중.
- **합류:** EVAL_YEARS·available_years 디스크발견으로 2026 서빙 가능화. H-2 floor 널 p99≈0.517(2026,
  타당). H-4 serve_as_of_year 전환은 텍스트 완료 후. DECISIONS D-030 기록.
</content>

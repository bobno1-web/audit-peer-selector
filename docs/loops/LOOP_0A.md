# LOOP 0-A — 프로젝트 골격 (소급 복원)

> **소급 복원**: 이 파일은 사후에 `docs/LOOP_LOG.md` + 현재 산출물을 근거로 재구성했다.
> 원 지시서는 채팅에만 있었다(파일화 규칙은 Loop 0-D에서 신설). 근거 불충분 항목은 `[복원불가]`.

## 목적
디렉토리 골격만 생성. 로직 0줄.

## 완료조건 (복원)
1. 지정된 디렉토리 트리 생성.
2. `python -c "import engines, scoring"` 통과 (빈 패키지 OK).
3. 각 폴더에 README.md 존재.
4. 지시 외 파일/폴더 추가 0.

## 비고
- 빈 폴더 placeholder(README vs .gitkeep) 원문 표현: `[복원불가]`. 현재 산출물은 폴더별 README 방식.

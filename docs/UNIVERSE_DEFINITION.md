# UNIVERSE_DEFINITION — 최종 유니버스 정의 (Loop 0-E PART F)

숫자는 실측(`scripts/spike_universe_v2.py`, seed=20260714, n=200). 제외는 **구조적 근거만**(종목명 키워드 없음).

## 최종 유니버스
**상장사만 = KOSPI(corp_cls=Y) + KOSDAQ(corp_cls=K).** (비상장은 D-008로 제외.)

### 넣는 것 / 빼는 것 (구조적 근거)
| 대상 | 처리 | 구조적 근거 |
|------|------|-------------|
| 보통주 상장사 | 포함 | corpCode stock_code 6자리, 끝자리 0 |
| 우선주 | (자동 제외) | corpCode에 **별도 엔트리 없음** — 전부 대표 보통주로 통합(끝자리 전부 '0', 0-D 실측) |
| 금융·보험·부동산(리츠·펀드·스팩 다수) | 제외 | `induty_code` 접두 64/65/66/68 (스캔 231 중 31=13% 제외) |
| ETF·ETN·스팩(정밀) | **구조적 분리 불가** | DART 필드로 못 가름 → KRX 증권구분 필요(아래 확보 실패) |
| 코넥스(corp_cls=N) | 정책 보류 | 데이터상 재무 확보 낮음 |

## 정제 후 상장 확보율 (n=200 실측)
- **67.5% (135/200)**. (0-D 65%(n=100) → 0-E 67.5%(n=200), 일관.)
- 남은 결측 65건의 정체(구조적 진단):
  - **89% (58건) 신규상장/상폐/기타** — DART만으로 구분 불가(상장일·상폐일 없음).
  - 8% (5건) 코넥스, 3% (2건) 비12월결산.

## KRX 데이터 확보 (F-2, 실측)
**확보 불가.** 두 방식 모두 실패:
- `getJsonData.cmd` (MDCSTAT01901) → **HTTP 400**.
- `GenerateOTP → download_csv` → **0행** 반환.
- 결론: 프로그램적 자동 확보 실패(세션/OTP 우회 필요).
- **대안 제시**: (1) KRX 정보데이터시스템에서 **수동 CSV**(전종목 기본정보 + 상장폐지종목) 1회 다운로드,
  (2) KIND(kind.krx.co.kr), (3) 금융위 OpenAPI/데이터 벤더. 자동화는 추가 작업 필요.

## 생존편향 대응 (F-3) — ★ 명시적 보고
**현재 불가.** 근거:
- corpCode에 **폐지일 필드 없음**(0-D 실측), KRX 상폐일 **자동 확보 실패**(위).
- 상폐일이 없으면 "시점 T에 살아있던 기업"을 정확히 복원할 수 없다 → **생존편향 금지 규칙을
  완전히는 지킬 수 없다.** 숨기지 않고 명시한다.
- 지금 상태로 데이터를 만들면 **생존편향이 잔존**한다(현재 상장 중심).
- 해소 조건: KRX 상장폐지종목 목록(상장일·폐지일)을 **1회라도** 확보하면 규칙 준수 가능.
  그 전까지 이 한계를 데이터/결과에 반드시 표기한다.

## 남은 결측 요약
정제 후 32.5% 결측 = 대부분 신규상장/상폐(상장일·상폐일 부재로 DART 단독 구분 불가) + 소수 코넥스·비12월결산.
→ 유니버스를 더 깨끗이 하려면 **KRX 상장 메타데이터가 병목**이다(생존편향 해소와 동일 병목).
